# Render — Portfolio CMS (Node Web Service)
# If the service was created in the Dashboard, set Start Command there too.
services:
  - type: web
    name: portfolio-cms
    runtime: node
    plan: free
    branch: main
    buildCommand: npm run build
    startCommand: node scripts/serve-dist.mjs
